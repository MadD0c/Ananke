
[gcode_macro M600]
gcode:
        {% set defaultcfg = printer['gcode_macro _defaultcfg'] %} ; get defaultcfg variables
        {% if defaultcfg.m600 == smart %}
            SMART_M600 { rawparams }
        {% else %}
            MOM600  { rawparams }
        {% endif %}
    